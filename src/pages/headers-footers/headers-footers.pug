extends ../../layouts/ui-layout.pug
include ../../blocks/header/header.pug

block variables
  - const bodyStyle = "background:#F4F4F6";

block content
  - const { header } = require("./headers-footers.json");
  - const { menu, user } = header;

  .headers-footers
    .headers-footers__item
      +header(menu)
    .headers-footers__item
      +header(menu, "true", user)
    .headers-footers__item.headers-footers__item_background
    .headers-footers__item
